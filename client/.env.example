# Mapbox GL access token used to render the basemap.
# Get one at https://account.mapbox.com/access-tokens/
# Copy this file to .env.local for local development, and set the same variable
# as a build-time environment variable in Cloudflare Pages for production.
VITE_MAPBOX_TOKEN=
